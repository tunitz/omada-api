{
  "name": "omada-api",
  "version": "1.0.50",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts",
    "compile": "bun build --target=bun ./src/index.js --outfile=./build/index.js"
  },
  "dependencies": {
    "elysia": "latest"
  },
  "devDependencies": {
    "@flydotio/dockerfile": "^0.4.9",
    "bun-types": "latest"
  },
  "module": "src/index.js"
}